/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./src/js/index.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./src/js/index.js":
/*!*************************!*\
  !*** ./src/js/index.js ***!
  \*************************/
/*! no static exports found */
/***/ (function(module, exports) {

function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var getDate = function getDate(format) {
  var date = new Date();
  var replaces = {
    yyyy: date.getFullYear(),
    mm: ('0' + (date.getMonth() + 1)).slice(-2),
    dd: ('0' + date.getDate()).slice(-2),
    hh: ('0' + date.getHours()).slice(-2),
    MM: ('0' + date.getMinutes()).slice(-2)
  };
  var result = format;

  for (var replace in replaces) {
    result = result.replace(replace, replaces[replace]);
  }

  return result;
};

var dateFormat = 'dd-mm-yyyy hh:MM';

var randomString = function randomString(i) {
  var rnd = '';

  while (rnd.length < i) {
    rnd += Math.random().toString(36).substring(2);
  }

  return rnd.substring(0, i);
};

var notes = [{
  _id: randomString(8),
  date: getDate(dateFormat),
  text: 'Lorem ipsum dolor sit, amet consectetur adipisicing elit. Dignissimos, et.'
}];
var lastNotes = Object.values(JSON.parse(localStorage.getItem('notes') || false));

var inputArrOfNotes = function inputArrOfNotes() {
  return lastNotes.length == 0 ? notes : lastNotes;
};

(function (arrOfNotes) {
  var addBtn = document.querySelector('.note-add');
  var container = document.querySelector('.container');
  var objOfNotes = arrOfNotes.reduce(function (acc, note) {
    acc[note._id] = note;
    return acc;
  }, {});

  var renderAllNotes = function renderAllNotes(notesList) {
    if (!notesList) {
      console.error('Нет заметок');
      return;
    }

    var fragment = document.createDocumentFragment();
    Object.values(notesList).forEach(function (note) {
      var div = noteTemplate(note);
      fragment.appendChild(div);
    });
    container.appendChild(fragment);
  };

  var noteTemplate = function noteTemplate() {
    var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
        _id = _ref._id,
        date = _ref.date,
        text = _ref.text;

    var div = document.createElement('div');
    div.className = 'note fadeIn', div.setAttribute('data-id', _id);
    div.insertAdjacentHTML('afterbegin', "<div class=\"note-header\">\n                <div class=\"note-date\">".concat(date, "</div>\n                <button class=\"note-del\" title=\"\u0423\u0434\u0430\u043B\u0438\u0442\u044C \u0437\u0430\u043C\u0435\u0442\u043A\u0443\"></button>\n            </div>\n            <textarea class=\"note-textarea\" spellcheck=\"false\" contenteditable=\"true\">").concat(text || '', "</textarea>\n        "));
    return div;
  };

  var addNote = function addNote() {
    var date = getDate(dateFormat);
    var note = createNote(date);
    var item = noteTemplate(note);
    container.appendChild(item);
    item.scrollIntoView({
      behavior: 'smooth',
      block: 'start'
    });
  };

  var createNote = function createNote(date) {
    var newNote = {
      _id: randomString(8),
      date: date,
      text: ''
    };
    objOfNotes[newNote._id] = newNote;
    localStorage.setItem('notes', JSON.stringify(objOfNotes));
    return _objectSpread({}, newNote);
  };

  var deleteNote = function deleteNote(id, el) {
    delete objOfNotes[id];
    el.remove();
    localStorage.setItem('notes', JSON.stringify(objOfNotes));
  };

  var onDeleteHandler = function onDeleteHandler(_ref2) {
    var target = _ref2.target;
    if (!target.classList.contains('note-del')) return;
    var parent = target.closest('[data-id]');
    var id = parent.dataset.id;
    parent.classList.remove('fadeIn');
    setTimeout(function () {
      deleteNote(id, parent);
    }, 300);
  };

  var onTextChangeHander = function onTextChangeHander(_ref3) {
    var target = _ref3.target;
    if (!target.classList.contains('note-textarea')) return;
    var parent = target.closest('[data-id]');
    var id = parent.dataset.id;
    objOfNotes[id].text = target.value;
    localStorage.setItem('notes', JSON.stringify(objOfNotes));
  };

  addBtn.addEventListener('click', addNote);
  container.addEventListener('click', onDeleteHandler);
  container.addEventListener('input', onTextChangeHander);
  renderAllNotes(objOfNotes);
})(inputArrOfNotes());

/***/ })

/******/ });
//# sourceMappingURL=scripts.min.js.map